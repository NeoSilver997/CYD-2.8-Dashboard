<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Always dark. This is an appliance on a wall, not an app that follows the
         system theme, so nothing here or in Compose ever reads
         isSystemInDarkTheme(). The window background is black so there is no
         flash of another colour before the first frame is rasterised. -->
    <style name="Theme.CydClock" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
